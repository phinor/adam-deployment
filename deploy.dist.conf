# -----------------------------------------------------------------------
# Deployment Configuration for this Server
# -----------------------------------------------------------------------

# GitHub repository in "owner/repo" format
GITHUB_REPO="phinor/adam"

# Path to the file containing your GitHub Personal Access Token (PAT)
TOKEN_PATH="/home/adamadmin/.github_token"

# Base directory for the application
APP_BASE_DIR="/var/www/adam"

# Number of old releases to keep
KEEP_RELEASES=5

# The user that owns the PHP-FPM process (e.g., 'www-data' on Ubuntu)
FPM_USER="www-data"